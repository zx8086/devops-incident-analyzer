# Handover: SIO-1784, SIO-1828, SIO-1829, SIO-1830 and SIO-1831, all closed 2026-09-20

| | |
|---|---|
| **Date** | 2026-09-20 |
| **Tickets** | **All Done:** [SIO-1784](https://linear.app/siobytes/issue/SIO-1784), [SIO-1828](https://linear.app/siobytes/issue/SIO-1828), [SIO-1829](https://linear.app/siobytes/issue/SIO-1829), [SIO-1830](https://linear.app/siobytes/issue/SIO-1830), [SIO-1831](https://linear.app/siobytes/issue/SIO-1831). Nothing open. |
| **Project** | [DevOps Incident Analyzer](https://linear.app/siobytes/project/devops-incident-analyzer-02f717a4b59a) |
| **Repo state** | `main` @ `5aac2a6b`, level with `origin/main`, clean tree |
| **Branch for next work** | None. The only follow-up is a fleet rollout, not a code branch. |

## TL;DR

SIO-1784 (deterministic ECS enumeration completion) and SIO-1830 (spoke reply
discarded) are **Done**: reviewed 5/5, on `main`, and closed 2026-09-20 with the
user's explicit approval.

They originally landed directly on `main` with no PR, bypassing the Greptile gate.
That gap is now **closed**: PRs #855 and #857 were opened retroactively against
temporary base refs, reviewed to 5/5, and their fixes cherry-picked to `main` as
`cf9858e5` and `7e68b5f9`. Five findings, three of them real bugs -- the
finding-by-finding record is a **comment on each Linear ticket**, because the PRs
were closed rather than merged.

**Everything in this handover is now closed.** SIO-1828 shipped as PR #858 and
SIO-1831 as PR #859 (Greptile 5/5), both merged. Nothing here is pending code work.

**The one live action left is a deployment, not a change:** SIO-1831 is merged but
`packages/pi-coms/extensions/` reaches spokes only through a bundle publish + rollout,
and its acceptance asked for a dev check (`just coms eu-shared-services-dev ...`)
first, since it changes what every spoke returns.

## What shipped

### SIO-1784 -- complete the ECS enumeration (`21fe0a12`, `d20bf38e`)

`packages/agent/src/aws-absence-completion.ts` walks the clusters the model left
unpaginated, so a pi verify-card is suppressed on a *proven* absence rather than
on whatever the model happened to enumerate.

The trap, which the ticket's own original proposal got wrong and its correction
section fixes: **the completion must call the UNINSTRUMENTED tools.** The
duplicate check at `sub-agent-loop-guard.ts:857` (`seenSignatures.has(signature)`)
sits BEFORE the `RUN_BACKSTOP_EXEMPT_TOOLS` carve-out at `:862`, so re-listing
page 1 of a partly-walked cluster is refused as an exact duplicate. Pages are fed
back into the ledger through the `observeEcsPage` seam on `ctx.runSignals`
(`sub-agent-instrumentation.ts`), not through the tool proxies.

Deviation from the handover that specified this: it suggested exporting the raw
`ECS_TOKEN_FIELDS`; I exported `nextEcsToken()` instead, so the token-field
knowledge stays in one place.

Caps: `MAX_COMPLETION_CLUSTERS = 10`, `MAX_COMPLETION_PAGES_PER_CLUSTER = 5`.

### SIO-1830 -- adapt the spoke's `diagnoses` envelope (`14a679a2`, `66f2c7c3`)

A live `eu-oit-prd` spoke returned a correct, well-evidenced diagnosis under a
`{diagnoses:[...]}` envelope. Both `PiVerdictSchema` and `PiInvestigationSchema`
rejected it, and the answer was **silently discarded** behind one `warn`.

Fix is a SEPARATE schema plus adapter in `packages/shared/src/pi-coms-types.ts`
(`PiDiagnosesReplySchema`, `investigationFromDiagnoses`), tried as a third branch
in `pi-verifier.ts:583` after the existing two. Not a loosening of either existing
schema -- five malformed payloads are tested and still rejected.

Mapping worth knowing: `evidence[].command` becomes `evidence[].resource` (the
spoke records the command it ran, not an arn), `affected_resources` is *prepended*
as evidence or it would be dropped entirely, and absent `confidence` defaults to
**0.5**, never 1, so silence is not read as certainty.

Both rejection warns now log `responseKeys: Object.keys(response)`. **Keys only,
never values** -- the body carries account ids, arns and trace ids. This mattered
concretely: diagnosing the original required hand-pulling the message from the
hub, and it had **aged out within the hour** (`message_not_found` on re-fetch).

## The review gap is CLOSED (was the open decision; resolved 2026-09-20)

Both tickets originally reached `main` with **no PR**, so neither passed the `Greptile Review`
gate. That has since been fixed retroactively and needs no further action.

Because the code was already on `main`, a PR against `main` computes an **empty** diff (and
shows the later docs commits being deleted). So each PR was targeted at a temporary base ref
holding the pre-change commit, rebuilt from the ORIGINAL feature commits:

| PR | Base | Result |
|---|---|---|
| [#855](https://github.com/zx8086/devops-incident-analyzer/pull/855) SIO-1784 | `review-base-sio-1784` (= `f3da4d80`) | **5/5**, 3 rounds, all 7 checks green |
| [#857](https://github.com/zx8086/devops-incident-analyzer/pull/857) SIO-1830 | the SIO-1784 branch | **5/5**, 2 rounds, all 7 checks green |

Five findings were raised; three were real bugs, one was half-right, one was a third right.
The fixes are on `main` as `cf9858e5` (SIO-1784) and `7e68b5f9` (SIO-1830). **The
finding-by-finding record is a comment on each Linear ticket** -- the PRs were CLOSED rather
than merged (their bases were throwaway refs), so that is where to look, not GitHub.

The one that mattered: **the SIO-1830 adapter read `diagnoses[0]` while the schema accepts N**,
silently dropping every later cause while reporting success. The monitor sends several findings
per request and asks for "one entry per dedup_key" (`coms-net-monitor.ts:505`), so multi-entry
replies are the producer's normal output. Now aggregated, with confidence taking the **minimum**
across causes so one certain cause cannot mask a guess.

All four temporary refs are deleted. **Closing a PR knocks its Linear issue from In Review back
to In Progress** -- both were restored.

## Verification -- what I actually ran, just now

| Command | Result |
|---|---|
| `bun run typecheck` | **clean**, 0 errors (2680 files in web) |
| `bun run lint` | **7 errors, 18 warnings -- all pre-existing** (was 8; see below) |
| `cd packages/agent && bun run test` | **4843 pass, 0 fail** (245 files) |
| `cd packages/shared && bun run test` | **557 pass, 80 fail** -- the 80 are pre-existing |
| `cd packages/shared && bun run test src/pi-diagnoses-adapter.test.ts` | **11 pass, 0 fail** |

Re-measured on `main` @ `7e68b5f9`, after the review fixes and after PR #856 landed.
The agent count includes #856's tests plus the 3 new deadline tests; the shared count
includes the 2 new multi-diagnosis tests.

**One lint error was mine and is now fixed** (`835f4d1b`): the SIO-1784 e2e test
landed indented with spaces where the repo uses tabs. Whitespace only.

The other 7 are pre-existing, proved per-file rather than asserted -- none of those
files appear in `git log 840a5e62..66f2c7c3`. The one that looks like mine,
`sub-agent.ts:87 useOptionalChain`, traces via `git log -L` to **SIO-1227 / PR #478**,
not to this work.

The 80 `packages/shared` failures are the OAuth (35), JSON-RPC retry (12),
`hasSeededTokens` (7), `waitForOAuthCallback` (6) and agentcore-proxy (15) suites.
Same count as recorded in SIO-1830 before these changes.

**Use `bun run test`, never bare `bun test`** -- the script carries `--isolate`,
and without it you get ~24 false failures in `packages/agent`.

## What is NOT verified (carried past Done deliberately)

Two items were carried past Done on the user's approval, recorded here so they are
not lost with their tickets.

**SIO-1831 is merged but NOT deployed.** `packages/pi-coms/extensions/` reaches spokes only
through a bundle publish + rollout, so production is unchanged. Its acceptance asked for a
dev check first (`just coms eu-shared-services-dev ...`) because it changes what every spoke
returns. Read `packages/pi-coms/docs/deployment/deployment.md` before that rollout.

**SIO-1784 has one acceptance item unmet:**


The SIO-1784 **lazy half** -- completion actually firing on a real
partly-enumerated run -- is proven only by `aws-absence-completion.e2e.test.ts`,
which drives the real `instrumentTools` closure and the real ledger. Three live
replays established the **safety** half (completion never fires when the proof
already holds); no natural run produced the condition the completion exists for.

A caution from this session, worth repeating: on one attempt I nearly filed a bug
against working code because **my probe** trimmed the ledger on every observed page
including completion pages. The probe was the bug. Suspect the probe first.

## SIO-1831 -- DONE (PR #859, Greptile 5/5), merged but NOT yet rolled out

Shipped: `buildTurnReplies` now validates the reply against the `response_schema` it was
handed. Two things from the review are worth carrying, both counter-intuitive:

- **typebox never throws on a schema it cannot interpret, it returns `false`**, so a
  try/catch fail-open guard does nothing. Validation is limited to an object schema naming
  required keys, or one bad schema would fail-shut every spoke.
- **My first mutation check passed for the wrong reason.** The fixture was an abbreviated
  schema, so every case exited on an earlier required-key guard and `Value.Check` was never
  reached; replacing it with `true` left all 30 tests green. Greptile caught it.

The original analysis follows, kept because it explains why the reader-side fix alone was
not enough.

**Do not start this without claiming it first** (it is Todo, unassigned).

The finding that makes it actionable: `packages/pi-coms/extensions/turnReply.ts:160`
already branches on `inbound.response_schema` and already extracts JSON --

```ts
} else if (inbound.response_schema && typeof inbound.response_schema === "object") {
    const parsed = extractJsonPayload(lastAssistantText);
    if (parsed === undefined) {
        replies.push({ msg_id: inbound.msg_id, response: null, error: notJsonError(final) });
    } else {
        replies.push({ msg_id: inbound.msg_id, response: parsed, error: null });
    }
}
```

-- but it only checks the reply **is JSON**, never that it **matches the schema it
was handed**. A shape mismatch returns `error: null` and fails 90 s later at the
analyzer, where nothing can be done but log a warn.

**Why a persona-only fix is the wrong instinct here:** `coms-net.ts:813` ALREADY
injects the schema into the recipient's turn content, with a comment saying "both
account agents guessed the same wrong shape". That mechanism has already failed in
production. A third bet on it is not the recommendation; validating at the seam is.

Constraints: any change under `agents/pi-fleet/` or `packages/pi-coms/extensions/`
needs a **bundle publish + rollout** to reach spokes -- read
`packages/pi-coms/docs/deployment/deployment.md` first, never reconstruct it from
`scripts/fleet.ts`. The non-schema path must keep returning prose verbatim
(`turnReply.ts:167`). The SIO-1830 adapter **stays** either way; it is the net for
dialects not yet seen and for spokes on older bundles.

## SIO-1828 -- DONE (PR #858), shipped by a parallel session

**Closed. Merged as `bc437d4d` while SIO-1831 was in progress.** I validated the premise
here, then found the PR already merged before writing any code, so nothing was duplicated.
The implementation matches what this section prescribes: a synthesised minimum-entropy ULID
floor (`windowFloorCursor`), forward paging capped at `MAILBOX_MAX_PAGES = 5`, a `truncated`
flag for the honesty criterion, and a fallback to the old read when `from` is unparseable.

The analysis below stands as the explanation of the defect.

**Todo, High, unclaimed.** This is still the reason older incident windows read
empty, and it is *separate* from the SIO-1825 header-parsing bug already merged.

`fetchFleetInbox` reads each mailbox with a fixed `limit: 100` and **no cursor**
(`fleet-inbox-node.ts:63`, `MAILBOX_READ_LIMIT`), and the hub with no `since`
returns the **newest** `limit` rows (`coms-net-server.ts:634`,
`ORDER BY msg_id DESC LIMIT ?`). `withinWindow` then filters client-side over
whatever those 100 rows happen to be. So the reachable history is not a time
window at all, it is "however far back 100 messages happen to stretch", which
moves with traffic.

Measured on the live prd hub 2026-09-20: the `ops` inbox (shared by six accounts'
monitors) covers only **26.8 h** at `limit=100`, back to 2026-09-18 20:30. A run
whose window was 2026-09-16 19:10 to 2026-09-18 19:10 therefore had **zero** of its
100 rows inside the window, and would read `0` even with SIO-1825 fixed.

**The trap:** "just pass `since`" is wrong. `since` is a ULID cursor that pages
**forward** (`msg_id > ? ORDER BY msg_id ASC`), so it returns only *newer* rows.
The real fix synthesises a minimum-entropy ULID at `window.from` and pages forward
until `created_at` passes `window.to` -- a paging loop with a page cap and a
total-row cap, all inside the existing per-estate `fleetInboxTimeoutMs`. Raising
the limit is rejected: the hub caps it at 100 (`pi-coms-client.ts:522`) and it
only moves the cliff.

When a cap truncates the read the digest must say so, per the same honesty rule
SIO-1815/#846 applied to the entry cap. A silently short read is the failure mode
this ticket exists to remove.

## SIO-1829 -- DONE (PR #856), kept for context only

**Closed 2026-09-20. Do not re-open it as "intermittent verify-with-pi"; the question was
decided.** It was rescoped: the verify path deliberately does **not** adapt a diagnoses reply,
because a verdict is a judgement on the analyzer's claims while a diagnosis is the spoke's own
observation, and the observed reply diagnosed a *different service* than was asked about.
Synthesising one from the other would invent a judgement the spoke never made. Instead the
failure message now names the shape received and points at investigate-with-pi, which does read
it. The rest of this section is the original analysis, kept because it explains the envelope.

Same `msg_id` (`01M2Y0YER09EBRCNV4X1S5QTHH`), same `eu-oit-prd` spoke, same
`{diagnoses:[...]}` envelope. Where SIO-1830 was "the analyzer discarded a good
reply", this is the operator-visible half: the card reads
`pi agent eu-oit-prd replied with an unusable verdict (schema mismatch)`.

Two facts that shape the investigation, and neither is a formatting problem:

1. **It is intermittent, updated 2026-09-20.** A run ~50 minutes later against the
   same spoke returned a correctly-shaped `PiVerdictSchema` verdict (`confirmed`,
   five claims with evidence) and the card rendered normally. So the spoke *can*
   honour the verdict schema. That rules out "the verify path never sets
   `response_schema`" as a standing condition.
2. **The failing reply was about the wrong service.** The analyzer asked about
   `localcore-service`; the returned diagnosis was about `orders-service` /
   `ImageEventConsumer.java:36`, keyed to an orders log group. That is
   `DIAGNOSIS_RESPONSE_SCHEMA` (`monitor/report.ts:105,146`) -- the schema the
   **monitor** sends. So this reads less like bad formatting and more like a
   monitor turn's reply being delivered against the analyzer's `msg_id`.

Ruled out as a cause: the failing run executed `840a5e62`, and `f3da4d80`
(SIO-1825) is not an ancestor of it; `PiVerdictSchema` is byte-identical between
the two; the guard itself predates this work (SIO-1635, PR #682).

**Reproduction needs overlap, not repetition** -- try to run an analyzer verify
request while a monitor investigation is in flight on the same spoke, rather than
expecting failure on every call.

**Note the interaction with what shipped:** SIO-1830's adapter now accepts the
`diagnoses` envelope on the *investigation* path, so a repeat of this may surface
differently than the original "Failed" card. The `responseKeys` diagnostics added
in `66f2c7c3` are exactly what lead 3 of this ticket asked for, so the next
occurrence should be self-describing without hand-pulling from the hub. **Re-read
the failure with those diagnostics before assuming the old analysis still holds.**

## Housekeeping done

- Killed my orphaned replay server: vite PID 52705 / parent 52703, started 02:30
  from the `sio1825-verify` worktree. Ports **5174 and 9087 proved free** afterwards
  (9087 was the in-process KG server inside that same vite process).
- **Left running, not mine**: `bun` on 9080 and 3001 (both started 16:13, before this
  session's work) and the `session-manager-plugin` SSM tunnel on 8788
  (`eu-shared-services-prd`, the known `fleet` CLI orphan pattern). Kill those only
  if you know they are yours.
- Two untracked files left untracked deliberately: `.mcp.json` and
  `packages/pi-coms/deploy/fleet.yaml.bak-sio1741`. `.mcp.json` was swept into PR #852
  by a `git add -A` earlier in this session and caused its only CI failure -- do not
  let it in again.

## Memory references

`reference_bun_test_isolate_kills_mock_module_pollution`,
`feedback_fake_clients_certify_assumptions`,
`feedback_never_blame_working_code_for_probe_failures`,
`reference_git_remote_ops_fail_in_sandbox`,
`feedback_always_kill_own_background_processes_safely`,
`reference_fleet_cli_orphans_ssm_tunnels`,
`reference_isolated_web_replay_from_worktree`,
`feedback_never_create_linear_done`,
`reference_greptile_active_again_2026_09_14`,
`reference_retroactive_pr_needs_a_temp_base_ref`,
`reference_schema_injected_into_turn_content_still_ignored`,
`hub_pi_fleet`, `hub_pi_coms_ops`.
