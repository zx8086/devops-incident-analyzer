# HANDOFF: SIO-1127 -- HIL learning Phase 2: binding corrections + heuristics + PR-gated draft runbook

- **Date**: 2026-07-17
- **Ticket**: [SIO-1127](https://linear.app/siobytes/issue/SIO-1127/hil-learning-phase-2-binding-corrections-heuristics-pr-gated-draft) (Todo, Medium)
- **Parent epic**: [SIO-1125](https://linear.app/siobytes/issue/SIO-1125/hil-learning-loop-chat-triggered-learn-from-ticket-123-kg-memory)
- **Repo state**: `main` @ `6ffc9ab` (Phase 1 fully merged: SIO-1126/1130/1131/1132/1134, PRs #392/395/396/397/398)
- **Suggested branch**: `claude/sio-1127-hil-phase-2`

## TL;DR

Phase 1 shipped the `learn from TICKET-123` lane end-to-end for root causes and memory facts. Phase 2 activates the two proposal classes the distiller already emits but `applyLearnings` currently skips with "class not applied until SIO-1127": telemetry **binding** corrections (confirm/invalidate) and **heuristics** (skill proposals), plus a PR-gated draft runbook. The design is fully specified -- do not re-design; implement against the spec + Linear scope and account for the Phase 1 deltas listed below.

## Context

Primary design doc: `docs/superpowers/specs/2026-07-16-hil-learning-loop-design.md` (Phase 2 section) and the Linear description (kept current, includes the DEVOPS-1355 exercise). The plan's "Writers on approval" section specifies byte-parity with the confirm-binding CLI and the memory-pr `"runbook"` kind.

## Phase 1 deltas the spec does NOT know about (critical)

1. **Curated memory (SIO-1134)**: `Incident.ticketKey` now gates enrichment; `applyLearnings` curates on apply **only when >= 1 item is approved** -- and the gate at `packages/agent/src/learn/apply.ts:203-208` ALREADY includes `proposal.bindings` / `proposal.heuristics` ids, so Phase 2 items count toward curation automatically. Mirror-fact timing may also have moved by the time you start -- check whether [SIO-1135](https://linear.app/siobytes/issue/SIO-1135/purge-stale-uncurated-incidents-write-kg-mirror-facts-at-curation-time) landed and follow its fact-at-curation-time pattern for `kg-binding` / `kg-binding-invalidated`.
2. **Evidence grounding (SIO-1131)**: `verifyProposalEvidence` in `packages/agent/src/learn/distill.ts` drops ANY item whose evidence quotes don't ground in the exact distiller prompt text (every ellipsis fragment must ground). Binding items carry `resourceId`/`locator` -- the distiller prompt must demand VERBATIM identifiers from the ticket or the verifier will silently drop them (`droppedIds` warn log). Add a binding-focused test against the sanitized fixture.
3. **Exec-summary embedding (SIO-1132)**: `buildMatchEmbedText` in `learn/match.ts` embeds summary + Executive Summary only; irrelevant to bindings but explains match behavior in fixtures.
4. **Existing skipped-item contract**: `packages/agent/src/learn/apply.ts:220-226` is the block Phase 2 replaces:

```ts
// Phase 2 (SIO-1127) activates these classes; a Phase 1 distiller emits them
// empty, but a hand-crafted resume must not silently drop approved items.
for (const item of [...proposal.bindings, ...proposal.heuristics]) {
    if (approved(decisions, item.id)) {
        report.skipped.push({ id: item.id, reason: "class not applied until SIO-1127 (Phase 2)" });
    }
}
```

5. **Rel-table lifecycle DDL (SIO-1136)**: fresh stores now declare `DEFAULT ''` on lifecycle columns in the CREATE DDLs. Any new writer touching `tInvalid` must match both CREATE and ALTER shapes (memory `reference_kg_create_ddl_needs_same_defaults_as_alters`).

## Scope (from Linear -- implement as written)

- `invalidateBindingByHuman` writer in `packages/knowledge-graph/src/writer.ts` (existing `invalidateBinding` hard-filters human edges by design; new one sets `tInvalid` + appends reason to evidence; single-clause lbug-safe Cypher) + knowledge-graph unit tests + `ladybug.integration.test.ts` coverage.
- Binding class end-to-end: confirm via `recordServiceBinding(confidence 1.0, discoveredBy "human", evidence "hil:<ticket>")` -- byte-parity with the confirm-binding CLI (`packages/agent/src/confirm-binding.ts` area, plan cites `:143-154`), re-validate kind with `BindingKindSchema`. Mirror facts `kg-binding` / `kg-binding-invalidated` with upgrade-aware dedup + rebuild.ts replay entries.
- Heuristics -> skill proposal facts via `buildSkillFactText`/`buildSkillAnnotations` from `packages/agent/src/skill-learner.ts` with `learned_from: "ticket:<key>"`; export the existing `proposalExists` for dedup; promotion stays `bun run skill:promote`.
- PR-gated draft runbook: extend `packages/memory-pr/src/types.ts` proposal kind with `"runbook"`; new `packages/agent/src/learn/runbook.ts` (DRAFT banner); immediate `promoteToMemory` so the PR URL lands in the apply summary; `RESOLVED_BY` links the draft filename. NEVER write into `agents/incident-analyzer/knowledge/runbooks/` directly -- the directory scan auto-catalogs on merge; the PR gate is the only control.
- UI: `LearningProposalCard.svelte` already renders generic proposal items; verify binding/heuristic sections render (they were schema'd in Phase 1) and extend labels as needed.

## Verification

```bash
bun run typecheck && bun run lint && bun run test
cd packages/knowledge-graph && bun test src/ladybug.integration.test.ts   # real engine for the new writer
```

Manual: `learn from DEVOPS-1355` at localhost:5173 -- expect binding + heuristic items on the review card with grounded evidence; approve; verify `OBSERVED_IN` edge confidence 1.0 / invalidated edge `tInvalid` set (KG MCP tools on :9087 or direct reader), the skill proposal fact, and the memory-PR URL in the apply summary.

## Workflow

Branch off `main`; commits `SIO-1127: ...` (HEREDOC + Co-Authored-By trailer); PR ready-for-review; triage CodeRabbit findings before the user's merge instruction; UI-verify after merge. Linear In Progress -> In Review; Done only with explicit user approval.

## Risks and edge cases

| Risk | Likelihood | Mitigation |
|---|---|---|
| Binding evidence quotes fail the verifier | High until prompt tuned | verbatim-identifier prompt rule + fixture test asserting zero droppedIds for bindings |
| Human-binding writes vs `KG_BINDINGS_WRITE_ENABLED` | Low | plan decision: human writes deliberately BYPASS the gate (CLI precedent) |
| Public repo fixtures | Certain if copied from real tickets | sanitize infra ids (synthetic sg-/vpc-/lkc- equivalents); ticket keys alone are fine |
| Web mocks of `@devops-agent/agent` | Certain for new exports | update the per-file mock factories (tests run `--isolate`, only touched files need it) |

## Out of scope

- SIO-1128 (Phase 3: rebuild replay closure for the remaining fact kinds, per-item edit UX, `KG_BINDINGS_READ_DATASOURCES` widening) -- next phase, spec-covered.
- SIO-1133 / SIO-1135 -- separate handoffs in this directory.

## Related code references

- `packages/agent/src/learn/apply.ts:112-226` -- the writer scaffold Phase 2 extends
- `packages/agent/src/learn/distill.ts` -- proposal schema prompt + evidence verifier
- `packages/shared/src/hil-learning.ts` -- `LearningProposalSchema` (bindings/heuristics already typed)
- `packages/agent/src/record-bindings.ts` -- W8 binding-write idiom (soft-fail, additive)
- `packages/knowledge-graph/src/rebuild.ts:179-182` -- replay ordering to extend

## Memory references

`reference_hil_learning_lane_sio1126`, `reference_kg_create_ddl_needs_same_defaults_as_alters`, `reference_lbug_rel_table_alter_supported`, `reference_skill_promotion_and_confidence`, `reference_incident_kg_enrichment_not_fanout`, `reference_agent_memory_recall_dedup`, `feedback_repo_is_public_sanitize_before_commit`, `feedback_plan_authority_over_pattern`
