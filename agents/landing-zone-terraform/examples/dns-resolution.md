# DNS is part of the post-vending network chain

Tags: DNS, Route 53, post-vending, network topology

User: Explain the DNS resolution path applied after a workload account VPC is vended.

Expected behaviour:

Inspect `aws-lz-network-workloads` and `aws-lz-post-vending`, plus core DNS only when the shared path is relevant. Describe VPC DNS settings, resolver rules or endpoints, associations, routes, and the post-vending dependency order with citations.

Do not treat a route as proof of DNS resolution. Mark any missing live account, VPC, subnet, route, or DNS state as unverified.
