node_modules/
.DS_Store
*.swp
*.swo

# API keys -- never commit real credentials
.env

.pi/agent-sessions/
tmp/

# Terraform: providers, state, and per-account variable files stay local
**/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars

# Fleet manifest: subnets, CIDRs, hub IPs and account ids stay local (SIO-1653)
deploy/fleet.yaml
deploy/accounts/*/backend.hcl
