# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
# Exception: CloudWatch Logs MCP tool family lives in a directory named logs
!packages/mcp-server-aws/src/tools/logs/
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Local reference implementations (not part of this repo)
migrate/

# Local reference material and ad-hoc artifacts (defect reports, scratch work)
current/

# Local git worktrees (kept outside the indexed tree)
.worktrees/

# Per-session handover docs live in experiments/ and commit directly to main.
# The directory used to be gitignored; that rule is removed so handovers are
# discoverable via git log from any branch/worktree.

# Local-only build artifacts that exceed GitHub's per-file size limit
*-mcp-agentcore.tar.gz

# SvelteKit generated output (regenerated by `svelte-kit sync` and dev/build)
.svelte-kit/

# Fallow incremental analysis cache
.fallow/

# Claude Code per-session state (plans, task outputs, transcripts) — local only
.claude/

# SIO-687 investigation: local-only eval entrypoint (DO NOT COMMIT)
packages/agent/src/eval/run-eval-no-precheck.ts

# Phase 1 IAM trust policy with developer-specific principal — do not commit
scripts/agentcore/policies/devops-agent-readonly-trust-policy-phase1.json
pvh-tf-state-pat.txt
